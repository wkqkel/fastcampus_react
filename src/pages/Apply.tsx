import Apply from '@/components/apply'

function ApplyPage() {
  const handleSubmit = () => {
    // 카드신청
  }

  return (
    <div>
      <Apply onSubmit={handleSubmit} />
    </div>
  )
}

export default ApplyPage
